/**
 * Represents a node in a graph with coordinates x and y.
 * @typedef {Array<number, number>} GraphNode
 * @property {number} 0 - The x coordinate of the graph node.
 * @property {number} 1 - The y coordinate of the graph node.
 */
export type GraphNode = [x: number, y: number];
